<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Book</title>
  <style>
    /* Simple pastel blue glassy styling similar to main */
    body {
      background: linear-gradient(to right, #d0e6f7, #c4e0fa);
      font-family: Arial, sans-serif;
      padding: 2rem;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background: rgba(255,255,255,0.5);
      border-radius: 12px;
      padding: 20px;
      backdrop-filter: blur(8px);
      box-shadow: 0 8px 32px rgba(31,38,135,0.1);
    }
    label {
      display: block;
      margin-top: 12px;
      font-weight: bold;
    }
    input[type=text], textarea, input[type=number] {
      width: 100%;
      padding: 8px;
      border-radius: 8px;
      border: none;
      background: rgba(230,240,255,0.8);
      box-shadow: inset 0 0 5px rgba(109,166,226,0.7);
      margin-top: 5px;
    }
    button {
      margin-top: 15px;
      padding: 10px 20px;
      background: #7ea4d7;
      border: none;
      border-radius: 20px;
      color: white;
      font-weight: bold;
      cursor: pointer;
    }
    button:hover {
      background: #5a7db1;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Edit Book</h2>
    <form action="/books/edit/<%= book.id %>" method="POST">
      <label for="title">Title</label>
      <input type="text" id="title" name="title" required value="<%= book.title %>" />

      <label for="author">Author</label>
      <input type="text" id="author" name="author" required value="<%= book.author %>" />

      <label for="isbn">ISBN</label>
      <input type="text" id="isbn" name="isbn" value="<%= book.isbn %>" />

      <label for="cover_url">Cover URL</label>
      <input type="text" id="cover_url" name="cover_url" value="<%= book.cover_url %>" />

      <label for="rating">Rating (1 to 5)</label>
      <input type="number" id="rating" name="rating" min="1" max="5" required value="<%= book.rating %>" />

      <label for="review">Review</label>
      <textarea id="review" name="review"><%= book.review %></textarea>

      <button type="submit">Update Book</button>
    </form>
  </div>
</body>
</html>
